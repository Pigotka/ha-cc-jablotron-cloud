  3. TimeoutError silently swallowed (__init__.py, lines 249–251)
    - _async_update_data catches TimeoutError and only logs warning
    - HA coordinator guidelines say transient errors should raise UpdateFailed(...) for retry
    - Means stale data served without marking entities unavailable
    - Severity: Medium
  4. _attr_unique_id collision risk (sensor.py line 83, climate.py line 113)
    - JablotronSensor: f"{service_id}_{thermo_device_id}"
    - JablotronClimate: f"{service_id}_{thermo_device_id}_climate"
    - If a device's can-control flag changes between setup and update, state updates go to wrong entity
    - Currently gated by can-control, but fragile design
    - Severity: Low (latent defect)
  5. async_set_hvac_mode doesn't update hvac_action (climate.py, lines 171–173)
    - Optimistic update sets hvac_mode but not hvac_action
    - Shows stale action state until next coordinator poll
    - Severity: Low
  6. _attr_name uses hardcoded English strings (All platforms)
    - sensor.py: f"{thermo_device_id}_temperature" (hardcoded _temperature)
    - climate.py: f"{thermo_device_id}_climate" (hardcoded _climate)
    - Should use translation_key with has_entity_name = True per HA guidelines
    - Severity: Low-Medium (guideline violation)
  7. supported_features as @property instead of _attr_ (alarm_control_panel.py, lines 117–123)
    - Returns static value based on init flag
    - Could be _attr_supported_features set in __init__
    - Minor style issue
    - Severity: Low (nitpick)